<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".CreateLanguageActivity">

    <!-- Toolbar -->
    <include
        android:id="@+id/toolbar_include"
        layout="@layout/toolbar_layout"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent" />

    <EditText
        android:id="@+id/et_language_name"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="80dp"
        android:hint="@string/nombre_del_lenguaje"
        android:inputType="textPersonName"
        app:layout_constraintTop_toBottomOf="@id/toolbar_include"
        android:layout_marginHorizontal="48sp"
        />

    <Button
        android:id="@+id/btn_save_language"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="40dp"
        android:text="@string/guardar_lenguaje"
        app:layout_constraintTop_toBottomOf="@+id/et_language_name"
        android:layout_marginHorizontal="96sp"
        />


</androidx.constraintlayout.widget.ConstraintLayout>